As per word2vec SVN revision 42, but with some modifications:

word2vec.c
===
in the case of negative sampling, write out the weights on the second layer syn1neg as well.
removed kmeans clustering code

